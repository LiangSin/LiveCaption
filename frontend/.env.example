# Streaming source
STREAM_URL=https://140.112.91.219:8088/live
RELAY_WS_URL=wss://140.112.91.219:8088/subtitles

# Frontend
FRONTEND_HOST=0.0.0.0
FRONTEND_PORT=8088

# Cookie signing secret; MUST be identical to AUTH_SECRET in nginx/.env
# (the njs verifier). Generate with: openssl rand -hex 32
AUTH_SECRET=change-me
