# ASR backend WebSocket endpoints.
#
# Each entry must be a host:port that accepts wss:// connections.
# After changing this file, restart or reload asr_proxy.
server 211.72.13.201:40685 max_fails=3 fail_timeout=10s;
server cool-parrot.mvnl.csie.ntu.edu.tw:8000 max_fails=3 fail_timeout=10s;
